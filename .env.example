# The PartyKit host the browser connects to.
# Local dev: leave unset (defaults to 127.0.0.1:1999 — run `npm run party:dev`).
# Production: your deployed PartyKit host, e.g. 5dice.<your-username>.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=
